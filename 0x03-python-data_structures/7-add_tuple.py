#!/usr/bin/python3


def add_tuple(tuple_a=(), tuple_b=()):
    new = []
    for i in tuple_a[:]:
        new[0].append = tuple_a[i] + tuple_b[i]
#        new[1].append = tuple_a[1] + tuple_b[1]
    return new
